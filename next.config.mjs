/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/spotify-2024-challenge-quiz",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
